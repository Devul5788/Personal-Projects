%2+2
%2^2
%xor(1,0)
% a=pi;
% disp(a)
% disp(sprintf('2 decimals: %0.2f', a))
% format long 
% a
% format short
% a

% A = [1 2; 3 4; 5 6]
% v = [1 2 3]
% v = [1; 2; 3]
% v = 1:0.1:2
% v = 1:6
ones(2,3)
% C = 2*ones(2,3)
% w = rand(1,3) %normal rand
%  w = randn(1,3) %normal dist.
%  w = log(w);
% if X is a matrix then X^2 means X*X (matrix mult). X.^X means squaring
% each element

% hist(w, 50)
% eye(5) %identity matrix
% help eye
% help rand
% help help